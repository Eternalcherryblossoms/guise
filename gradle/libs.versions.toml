[versions]
agp = "8.13.2"
kotlin = "2.4.20"

# Pinned to 101.0.0, NOT the newer 102.0.0.
#
# libxposed 102.0.0 declares minCompileSdk=37 in its AAR metadata, and this SDK
# installation cannot go past android-36.1 -- the SDK repository does not publish a
# platform 37 at all. 101.0.0 declares minCompileSdk=36 and, verified against its
# published sources, exposes every API this module uses (Hooker/Chain interception,
# ExceptionMode, remote preferences, remote files, and the full module lifecycle).
# The only 101 features given up are hook ids and hot reload, neither of which is used.
libxposed = "101.0.0"

# AndroidX pinned to the compileSdk-36 era. Newer releases declare
# minCompileSdk=37 in their AAR metadata and this SDK install only has 36.1.
coreKtx = "1.16.0"
activityCompose = "1.11.0"
lifecycle = "2.9.4"
composeBom = "2026.04.01"

kotlinxSerialization = "1.9.0"
junit = "4.13.2"

[libraries]
androidx-core-ktx = { module = "androidx.core:core-ktx", version.ref = "coreKtx" }
androidx-activity-compose = { module = "androidx.activity:activity-compose", version.ref = "activityCompose" }
androidx-lifecycle-runtime-ktx = { module = "androidx.lifecycle:lifecycle-runtime-ktx", version.ref = "lifecycle" }
androidx-lifecycle-viewmodel-ktx = { module = "androidx.lifecycle:lifecycle-viewmodel-ktx", version.ref = "lifecycle" }
androidx-lifecycle-viewmodel-compose = { module = "androidx.lifecycle:lifecycle-viewmodel-compose", version.ref = "lifecycle" }

androidx-compose-bom = { module = "androidx.compose:compose-bom", version.ref = "composeBom" }
androidx-compose-ui = { module = "androidx.compose.ui:ui" }
androidx-compose-ui-graphics = { module = "androidx.compose.ui:ui-graphics" }
androidx-compose-ui-tooling = { module = "androidx.compose.ui:ui-tooling" }
androidx-compose-ui-tooling-preview = { module = "androidx.compose.ui:ui-tooling-preview" }
androidx-compose-material3 = { module = "androidx.compose.material3:material3" }

# Modern Xposed API. `api` is compileOnly in :hook (the framework provides it at
# runtime); `service` is a real dependency of :app (it talks to the manager).
libxposed-api = { module = "io.github.libxposed:api", version.ref = "libxposed" }
libxposed-service = { module = "io.github.libxposed:service", version.ref = "libxposed" }

kotlinx-serialization-json = { module = "org.jetbrains.kotlinx:kotlinx-serialization-json", version.ref = "kotlinxSerialization" }
junit = { module = "junit:junit", version.ref = "junit" }

[plugins]
android-application = { id = "com.android.application", version.ref = "agp" }
android-library = { id = "com.android.library", version.ref = "agp" }
kotlin-android = { id = "org.jetbrains.kotlin.android", version.ref = "kotlin" }
kotlin-jvm = { id = "org.jetbrains.kotlin.jvm", version.ref = "kotlin" }
kotlin-compose = { id = "org.jetbrains.kotlin.plugin.compose", version.ref = "kotlin" }
kotlin-serialization = { id = "org.jetbrains.kotlin.plugin.serialization", version.ref = "kotlin" }
